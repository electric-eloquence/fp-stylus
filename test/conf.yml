express_port: 3000
livereload_port: 35729

# kill_zombies will kill old gulp processes lingering into a fresh launch of
# Fepper. However, if you wish to run multiple Fepper instances, you'll need to
# set this to false.
kill_zombies: true

app_dir: node_modules/fepper
backend_dir: backend
extend_dir: extend
